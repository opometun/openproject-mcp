# Runtime dependencies (mirrors pyproject.toml [project.dependencies])
mcp[cli]>=1.11.0,!=1.12.0,!=1.12.1
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional HTTP transport (pyproject optional-dependencies.http)
uvicorn>=0.20.0
starlette>=0.30.0

# Test/dev dependencies (pyproject optional-dependencies.dev)
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.3.0
mypy>=1.0.0
pre-commit>=3.5.0
respx>=0.20.0
